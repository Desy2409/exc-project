export const auth_user = {
    // asset: function(path) {
    //     return window._asset + path
    // },
    // route: function(path) {
    //     //alert(window._url)
    //     return window._url + path
    // },
    auth: window._auth,
    ev: window._event
}